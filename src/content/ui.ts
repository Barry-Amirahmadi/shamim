import type { UiStrings } from "@/types/content";

/**
 * Interface strings — accessible names, and the few words the page says on its
 * own behalf rather than the brand's. Separate from the copy deck because they
 * are edited for a different reason; neither belongs inside a component.
 */
export const ui: UiStrings = {
  skipToContent: "پرش به محتوای اصلی",
  /** Follows the brand name: «شمیم — بازگشت به آغاز». */
  home: "بازگشت به آغاز",
  launchLink: "اطلاع از عرضه",
  newWindow: "در پنجره‌ای تازه باز می‌شود",
};
